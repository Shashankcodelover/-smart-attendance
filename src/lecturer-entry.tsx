import React from 'react';
import ReactDOM from 'react-dom/client';
import LecturerApp from './lecturer-App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <LecturerApp />
  </React.StrictMode>
);
